name: Packages Split

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  packages_split:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package:
          - local_path: 'src/Components/Http'
            repository: 'munastack-http'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Настройка SSH ключа для пуша в целевой репозиторий
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan github.com >> ~/.ssh/known_hosts
          ssh-agent -a /tmp/ssh_agent.sock > /dev/null
          ssh-add - <<< "${{ secrets.HTTP_DEPLOY_KEY }}"
        env:
          SSH_AUTH_SOCK: /tmp/ssh_agent.sock

      # Используем актуальный экшен от Symplify
      - name: Split and Push
        uses: symplify/monorepo-split-github-action@v1.1
        with:
          package_directory: ${{ matrix.package.local_path }}
          package_repository: 'git@github.com:sinnerII/${{ matrix.package.repository }}.git'
          branch: main
          user_name: "sinnerII"
          user_email: "pavel@example.com"
        env:
          SSH_AUTH_SOCK: /tmp/ssh_agent.sock
